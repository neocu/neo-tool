import Page from '../../components/page';
import './{className}.less';

class {className} extends React.Component {

  static renderPage() {
    return (<div>{name} page</div>);
  }

  render() {
    return (
      <Page className="neo-{name}">
        {{className}.renderPage()}
      </Page>
    );
  }
}

export default {className};
